package ac.at.fhcampuswien;


public class App {

    /**
     * Übung 1 (A)
     * Schreiben Sie ein Programm, das vom Benutzer zwei ganze Zahlen n1 und n2 einliest und
     * die Summe aller ganzen Zahlen von n1 bis n2 (inklusiv) ausgibt.
     * Wenn n1 oder n2 (oder beide) < 0 ist/sind, soll das Programm eine entsprechende
     * Meldung ausgeben (siehe Programmbeispiel).
     * Wenn beide Zahlen > 0 sind, aber n2 < n1 ist, soll auch eine Fehlermeldung ausgegeben werden.
     */

    public static void sumUp() {

    }


    /**
     * Übung 2 (C)
     * Schreiben Sie eine Methode compareNumbers, welche 2 Parameter vom Typ Integer entgegennimmt und
     * vergleicht und die größere Zahl zurückgibt.
     * Wenn beide Zahlen gleich sind, gibt die Funktion 0 zurück.
     * Haben beide Zahlen denselben Restbetrag bei einer Division durch 6, soll die kleinere Zahl
     * zurückgegeben werden.
     */

    /*
    //implement here
    public static int compareNumbers(int x, int y){

    }

     */

    /**
     * Übung 3 (B)
     * Schreiben Sie eine Methode digitSum, die vom Benutzer eine ganze Zahl einliest und die Summe der Ziffern in der Konsole ausgibt.
     * Es werden dabei nur ganze, maximal dreistellige Zahlen berücksichtigt.
     * Ist die eingegebene Zahl < 0 oder hat mehr als drei Stellen, soll in der Konsole "Invalid Number" ausgegeben werden.
     */

    //implement here

    /**
     * Übung 4 (A)
     * Schreiben Sie eine Methode printTriangle(), welche einen Parameter vom Typ Integer entgegennimmt. Die Methode hat keinen Rückgabewert.
     * Die Funktion soll ein links-ausgerichtetes Dreieck in der Konsole ausgeben, wobei der Übergabeparameter bestimmt, aus wie viele Zeilen
     * das Dreieck besteht.
     * Wenn der Übergabeparameter < 0 ist, soll eine Fehlermeldung ausgegeben werden.
     */


    /**
     * Übung 5 (B)
     * Schreiben Sie eine Methode weekdays, die auf Basis einer Zahl, den korrekten Wochentag zurückgibt.
     * Die Methode verfügt über einen Rückgabewert vom Typ String.
     * Als Parameter wird eine int Variable übergeben. Der Parameter bestimmt den Wochentag, der zurückgegeben wird (1=Monday bis
     * 7=Sunday).
     * Wird ein Wert < 1 oder > 7 übergeben, soll die Methode "Invalid day." zurückgeben.
     */

    //implement here

    /**
     * Übung 6 (D)
     * Schreiben Sie eine Methode incomeStatistics, zur Berechnung der Statistik monatlichen Einkommens. Dabei werden von der Konsole
     * solange ganze Zahlen (monatliches Einkommen) eingelesen, bis die Zahl 0 eingegeben wird. Wird ein ungültiges Einkommen
     * angegeben (< 0), erfolgt eine Fehlermeldung, das Einkommen wird nicht gezählt und die Eingabeaufforderung schreitet
     * voran. Es wird die Annahme getroffen, dass nur Ganzzahlen eingegeben werden.
     * Bei Beendigung der Eingabe wird der Einkommensdurchschnitt (Ausgabe auf 2 Nachkommastellen), die Summe, sowie die
     * Anzahl der Eingaben ausgegeben.
     * Wurden bis zur Eingabe von 0 keine validen Zahlen eingegeben wird ebenfalls eine Fehlermeldung ausgegeben.
     * Die Methode hat keinen Rückgabewert.
     */

    //implement here

    public static void main(String[] args) {
        //hier methoden aufrufen zum ausprobieren!
        //...
        //... jetzt
        // ... ja hier!
        // ... einfach ausprobieren!
    }
}
